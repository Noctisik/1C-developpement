&НаСервере
Процедура Расш1_КоличествоВСпискеПослеНаСервере()
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ КОЛИЧЕСТВО("+ ЭтотОбъект.Заголовок +".Ссылка) КАК Кол ИЗ Справочник."+ ЭтотОбъект.Заголовок +" КАК "+ ЭтотОбъект.Заголовок;
	Результат = Запрос.Выполнить();
	
	Выборка = Результат.Выбрать();
	Выборка.Следующий();
	
	КоличествоВСписке = Выборка.Кол;
КонецПроцедуры

// В основную форму в процедуру Процедура ПриОткрытии(Отказ) добавлен вызов процедуры подсчёта количества Расш1_КоличествоВСпискеПослеНаСервере();
__________________________________________________________________
//В основной форме
//Добавлено Сергеев И.С. 19.10.2021 для открытия в форме процедуры подсчёта количества сотрудников
&НаКлиенте
Процедура ПриОткрытии(Отказ)

	   Расш1_КоличествоВСпискеПослеНаСервере();
	   
КонецПроцедуры

#КонецОбласти
